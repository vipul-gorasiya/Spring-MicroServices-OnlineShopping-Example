spring:
  profiles:
    active: development
  application:
    name: Eureka-Server
  cloud:
    config:
      uri: http://localhost:5055
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false