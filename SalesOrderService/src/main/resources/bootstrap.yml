spring:
  profiles:
    active: development
  application:
    name: SalesOrder-Service
  cloud:
    config:
      uri: http://localhost:5055
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: myGroup
      